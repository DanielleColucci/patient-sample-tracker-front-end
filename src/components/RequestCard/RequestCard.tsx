import { Profile } from "../../types/models"

interface RequestCardProps {
  profile: Profile;
  handleUpdateAuthorization: (profile: Profile) => void;
  handleUpdateAdmin: (profile: Profile) => void;
}

const RequestCard = (): JSX.Element => {
  return (
    <div>
      
    </div>
  )
}

export default RequestCard